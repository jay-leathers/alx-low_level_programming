#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @a: first parameter
 * @b: second parameter
 * Return: always 0
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
